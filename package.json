{
  "name": "second-chance-salvations",
  "version": "0.5.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "dotenv": "^8.1.0",
    "moment": "^2.24.0",
    "thousands": "^1.0.1"
  },
  "scripts": {
    "start": "start-p start:**",
    "build": "run-p build:**",
    "start:lambda": "netlify-lambda serve src/lambda",
    "build:lambda": "netlify-lambda build src/lambda",
  },
  "devDependencies": {
    "netlify-lambda": "^1.4.5",
    "npm-run-all": "^4.1.5"
  }
}
